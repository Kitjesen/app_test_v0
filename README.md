# 大算机器人 OW 控制系统 (Flutter版)

这是机器狗控制应用的Flutter版本，采用了高端产品级的设计风格。

## 🎨 深色主题设计

最新版本已升级为**现代化深色主题**，对标高端产品设计：

- 🌙 **深色背景**：采用 `#0A0E14` 主背景，减少视觉疲劳，增加科技感
- 🌿 **绿色主题**：采用 `#7FFF7F` 为强调色，传递活力与生命力
- ⭕ **环形进度**：直观展示电量、温度等状态数据
- ⛶ **渐变卡片**：统一的深绿渐变风格，层次分明
- 📱 **纯图标导航**：极简风格的5图标底部导航栏

## 🛠️ 核心功能

- 🔌 **连接管理**：实时监控连接状态
- 🎮 **双摇杆控制**：左摇杆移动，右摇杆转向
- 📺 **实时图传**：查看机器狗摄像头画面
- 📊 **状态概览**：电量、温度、信号、姿态实时监控
- 🤖 **3D模型**：机器狗模型可视化（开发中）
- 📋 **任务系统**：预设动作组和自定义任务
- 👤 **个人中心**：设置、图库与账号管理

## 🚀 快速开始

### 环境要求
- Flutter SDK >=3.0.0
- Dart SDK >=3.0.0

### 安装依赖
```bash
flutter pub get
```

### 运行项目
```bash
# Chrome预览（推荐用于UI调试）
flutter run -d chrome

# 移动端运行
flutter run -d ios
flutter run -d android
```

## 📁 项目结构

```
lib/
├── main.dart                 # 应用入口 & 全局主题
├── models/                   # 数据模型
│   └── robot_state.dart      # 机器人状态定义
├── providers/                # 状态管理
│   └── robot_provider.dart   # 全局状态Provider
├── screens/                  # 页面视图
│   ├── home_screen.dart      # 首页
│   ├── control_screen.dart   # 控制核心页（含摇杆）
│   ├── task_screen.dart      # 任务列表
│   ├── profile_screen.dart   # 个人中心
│   ├── fullscreen_control_screen.dart # 全屏控制
│   └── onboarding_screen.dart # 引导页
└── widgets/                  # 通用组件
    └── joystick.dart         # 虚拟摇杆组件
```

## 🎨 设计规范

- **主背景色**: `#0A0E14`
- **卡片背景**: `#0F1419`
- **主要强调色**: `#7FFF7F` (亮绿)
- **深绿渐变**: `#1A2A1A` → `#1F3A1F`
- **文字颜色**: 白色 (主要), `#8A92A6` (辅助)

## 技术栈

- Flutter 3.x
- Provider (状态管理)
- Material Design 3 (Dark Theme)
- WebSocket (通信协议)
